<!DOCTYPE html>
<html>
<head>
  <title>OnlineShop - интернет-магазин на Ruby On Rails</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="page-header">
    <div class="languages-selector">

    </div>

    <h1>
      <%= link_to(t('layout.header.title'), root_path) %>
      <small><%= t('layout.header.slogan') %></small>
    </h1>
  </div>

  <nav class="navbar navbar-default">
    <div class="container-fluid">
      <div class="navbar-header">
        <a class="navbar-brand" href="#">
          <img alt="Brand" src="...">
        </a>
      </div>
      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <li><%= link_to(t('layout.header.cart'), cart_path, class: "btn btn-default navbar-btn") %></li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <li><%= link_to("РУ", {locale: 'ru'}, {class: "#{I18n.locale == :ru ? 'active' : ''} "}) %><%= link_to("EN", {locale: 'en'}, {class: "#{I18n.locale == :en ? 'active' : ''} navbar-right"}) %></li>
        </ul>
      </div>

    </div>
  </nav>

  <div class='container'>
    <ol class="breadcrumb">
      <li><%= link_to(t('layout.header.home'), root_path) %></li>
      <li><a href="#">Library</a></li>
      <li class="active">Data</li>
    </ol>

    <%= yield %>
  </div>

  <footer>
    <div>
      © OnlineShop & <%= link_to "Андрей Ерёмин, #{Time.now.year}", "http://aeremin.ru", target: '_blank' %>
    </div>
    <div>
      <% if current_user && current_user.is_admin %>
        <%= link_to t('layout.footer.admin_page'), admin_goods_path %> |
      <% end %>
      <%= link_to t('layout.footer.main_page'), root_path %>
    </div>
  </footer>
</body>
</html>
